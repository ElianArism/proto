export interface Sizes {
    _id: string;
    size: string;
    avaible: boolean;
}

export interface Brand {
    _id: string;
    name: string;
    avaible: boolean;
}

export interface Type {
    _id: string;
    type: string;
}

export interface Img {
    public_id: string; 
    path: string;
}